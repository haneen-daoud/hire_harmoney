import 'package:flutter/material.dart';
class EmpProfileDetails extends StatelessWidget {
  const EmpProfileDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
       child :Text("haneen daoud "),
      ),
    );
  }
}